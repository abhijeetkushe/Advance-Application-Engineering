package org.stable.sdk.model.device;

import org.stable.sdk.model.measurement.Measurement;
import org.stable.sdk.model.patient.Patient;

public class TestDevice {
	
	public static void main(String[] args) {
		//AbstractDevice abstractDevice = DeviceFactory.createDevice(DeviceType.BASOS,  "BASOS/A");

		//AbstractDevice abstractDevice1 = DeviceFactory.createDevice(DeviceType.RESPIRATORY_RATE,  "Resp/01/12");
		//Measurement measurement = abstractDevice1.getMeasurement("MANUAL", "15");
		
		//Patient p = new Patient();
		//p.getMeasurements().add(measurement);
		
	}

}
